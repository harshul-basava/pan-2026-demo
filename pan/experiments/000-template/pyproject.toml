[project]
name = "experiment-template"
version = "0.1.0"
description = "Template for new PAN 2026 experiments"
requires-python = ">=3.10"

# Add experiment-specific dependencies here
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
